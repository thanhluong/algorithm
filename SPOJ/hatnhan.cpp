#include <iostream>

using namespace std;

main()
{
    cin >> s;
    n = s.length();
    for (i=1; i*i <= n; i++)
    {
        t += s[i-1];
        if (!(n%i))
        {
            tt = "";
            for (j=1; j <= )
        }
    }
}
